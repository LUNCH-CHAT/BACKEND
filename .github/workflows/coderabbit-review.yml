name: 🤖 CodeRabbit 리뷰

on:
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches: [ main ]

jobs:
  coderabbit-review:
    name: CodeRabbit 리뷰
    runs-on: ubuntu-latest
    
    steps:
      - name: 🤖 CodeRabbit 리뷰 실행
        uses: coderabbitai/coderabbit-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          language: ko
          review-level: detailed