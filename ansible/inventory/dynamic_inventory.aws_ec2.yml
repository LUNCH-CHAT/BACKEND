plugin: amazon.aws.aws_ec2
regions:
  - ap-northeast-2
keyed_groups:
  # 'Role' 태그의 값을 그룹 이름으로 사용합니다.
  # 예: Role=app-server -> [app_servers] 그룹에 포함
  - key: tags.Role
    prefix: ""
    separator: ""
compose:
  # 서버 이름으로 Public IP 주소를 사용합니다.
  ansible_host: public_ip_address